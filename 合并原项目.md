# 合并原项目（上游 QuantumNous/new-api）规则

## 1. 背景与核心原则

本项目从 [QuantumNous/new-api](https://github.com/QuantumNous/new-api) fork，我们对支付/钱包/兑换码/计费做了自定义实现。

**唯一规则：凡支付/钱包/兑换码/计费自定义与上游冲突，一律保留本地（我们的）版本，绝不允许被上游覆盖。**

- 采用 **rebase 方案**：把本地自定义提交逐个重放到上游最新代码之上，历史保持线性。
- 冲突由 git 自动以「保留我们的版本」解决，**不做人工回归**。
- 未冲突的上游改动仍正常并入（上游无关模块的更新要保留）。
- rebase 会重写本地提交历史（提交 SHA 会变化），推送时需强制推送（`--force-with-lease`）。

## 2. 基础信息

| 项 | 值 |
|---|---|
| 上游仓库 | `https://github.com/QuantumNous/new-api.git` |
| 上游 remote 名 | `upstream` |
| fork 起点（基线） | `47ba9d2c63d6dcbf3a183ee421b136ee1b1331ed`（2026-08-14） |
| 当前分支 | `main` |

**本地自定义提交（当前已 squash 为 1 个提交，全部保留）：**

- 历史拆分提交已合并（部署修复 ×5、限流、兑换码档位与核销记账、显示兑换码本体、钱包计费历史弹窗等）——见第 5 节合并记录。

## 2.1 自定义改动合并为同一个提交（长期规则）

**唯一提交：所有本地自定义改动（支付/钱包/兑换码/限流/部署等）必须合并进同一个提交 `4c4a16cd`，绝不允许拆成多个自定义提交。**

每次新增/修改自定义功能时：

```bash
# 1) 正常写代码、提交到本地（提交信息随意，临时提交）
git add <改动的文件>
git commit -m "wip: 临时提交本次自定义改动"

# 2) 把它并入唯一自定义提交 4c4a16cd（保留原有提交信息）
git commit --fixup 4c4a16cd          # 或直接 amend 前一个临时提交
# 推荐：git rebase -i --autosquash 4c4a16cd^ 把 fixup 折叠进 4c4a16cd
# 更简单（如果只多了一个临时提交且它紧跟在 4c4a16cd 之后）：
#   git reset --soft 4c4a16cd && git commit --amend --no-edit
```

- 目标：rebase 上游前，本地始终是「`上游基线 + 1 个自定义提交 4c4a16cd`」的形态。
- 自定义提交的标题保持不变：`feat: 自定义支付/钱包/兑换码/限流/部署改动（本地 fork 定制）`。
- 推送仍按第 4 节规则：**由用户自行决定并执行**（`git push --force-with-lease origin main`）。

## 3. 一次性初始化（只做一次）

```bash
git remote add upstream https://github.com/QuantumNous/new-api.git
git fetch upstream
```

## 4. 每次合并的标准流程

前置：当前分支 `main`，工作区干净（无未提交改动）。

**合并只到本地：以下流程只把上游合并进本地分支，不推送。**

```bash
# 1) 拉取上游最新
git fetch upstream

# 2) 将本地自定义提交重放到上游最新之上，冲突一律保留我们的版本
git rebase -X theirs upstream/main

# 3) 若 rebase 中途出问题想放弃，用
#    git rebase --abort        （回到 rebase 前状态）

# 4) 更新本文档第 5 节合并记录
```

### 推送（由用户自行决定并执行）

- 上面的流程**只合并到本地**，不会推送。
- **推送由用户自行决定并执行**，文档/流程不代推。
- 若确认无误后要推送（rebase 重写了提交历史，推送必须用强制推送）：

  ```bash
  git push --force-with-lease origin main
  ```

### 关键：rebase 的 `-X theirs` 含义（与 merge 相反）

- **merge** 里 `-X ours` = 保留「本地」版本；
- **rebase** 里 `-X theirs` = 保留「我们正在重放的本地提交」版本。

两者方向相反，rebase 时**必须用 `-X theirs`** 才能保留我们的改动。若在 rebase 里误用 `-X ours`，会反过来保留上游版本，把我们的支付自定义覆盖掉。

- 它只对**产生冲突的代码块**生效，冲突时整块采用我们的版本。
- **未冲突的上游改动仍正常并入**（期望行为）。
- 若出现 delete/modify、rename 等特殊冲突，按「保留本地」原则处理：保留我们的文件与功能，必要时手动并入上游对应功能。

## 5. 合并记录（每次合并后填写一行）

| 日期 | 合并的上游 HEAD | 冲突文件 | 结果 |
|---|---|---|---|
| 2026-08-16 | `e2c7aa7b`（rebase 到上游最新） | `.github/workflows/ci.yml`（modify/delete，本地有意删除上游 CI，保留） | rebase 成功；本地 9 条自定义提交重放至上游之上，随后 squash 为 1 个提交（当前为 `4c4a16cd`，标题标识自定义改动，前身 `1442135c`/`1442135c` amend 而来）；支付/钱包/兑换码自定义全部保留；未推送 |
| 2026-08-16 | —（自定义改动，非合并上游） | 无 | 按第 2.1 节规则把「SESSION_SECRET 部署改动」amend 进唯一自定义提交 `4c4a16cd`；docker-deploy workflow 增加 `SESSION_SECRET`（GitHub Secret，未配置则部署失败）与部署前保护；未推送 |

## 6. 备注

- 历史说明：2026-08-16 曾用 `git merge -X ours` 合并过上游 `e2c7aa7b`，因策略不合用已整体还原；自即日起改用 rebase 方案。
- 合并流程只操作本地分支，**推送由用户自行决定并执行**，文档/流程不代推。
- rebase 后若发现构建/功能异常，优先检查支付相关文件，确认是否有上游与本地不兼容的改动；处理结论同步更新保护清单。

## 7. SESSION_SECRET 部署规则

- **必须配置固定的 `SESSION_SECRET`**（GitHub Secret），并在部署 workflow（`.github/workflows/docker-deploy.yml`）里注入容器环境变量。
- 原因：未配置时进程每次启动随机生成 secret（`common/init.go`），**每次重新部署都会把所有用户登录会话全部踢下线**（Access Token/Refresh Token 签名全部失效）。
- 已加保护：若 GitHub Secret `SESSION_SECRET` 未配置，部署 workflow 直接失败，绝不静默用随机值启动。
- **变更 secret = 全员强制重登**：第一次配置固定值 / 更换固定值的那次部署，所有在线用户会重登一次（属预期）；之后只要值不变，重新部署不再踢人。挑低峰期做切换。
- 生成随机值：`openssl rand -hex 32`。
- 多实例/多节点部署时，所有节点必须使用**同一个** `SESSION_SECRET`（配合同一个主数据库）。
